Arrumando o git nessa bagaceira:

Acionei o comando `Plugs: Add` – e como _Plug URI_ – entreguei `github:silverbulletmd/silverbullet-git/git.plug.js`

Coloquei o seguinte na página SETTINGS:

```yaml
git:
  autoCommitMinutes: 5
  autoSync: false
```

Acionei o comando `Plugs: Update` e ao infinito e além.

Porém ainda não vejo o diretório `.git` na raiz da árvore de arquivos.

Será que preciso arrumar manualmente?
Ou só esperar 5 minutos?
Vamos ver!